# ============================================
# PROTOCOL OS - ENVIRONMENT CONFIGURATION
# ============================================
# Clone this file to .env and replace YOUR_X_HERE values
# Only ONE database provider should be active at a time
# ============================================

# --- APPLICATION CONFIGURATION ---
VITE_APP_BASE_URL=http://localhost:5173
VITE_APP_CALLBACK_PATH=/auth/callback
VITE_APP_VERSION=1.0.0

# --- DATABASE PERSISTENCE PROVIDER ---
# Set which provider is active (only one should be uncommented)
VITE_ACTIVE_DATABASE_PROVIDER=localStorage

# --- SUPABASE CONFIGURATION ---
# Uncomment and configure if using Supabase
# VITE_SUPABASE_PROJECT_URL=YOUR_SUPABASE_PROJECT_URL
# VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# --- FIREBASE CONFIGURATION ---
# Uncomment and configure if using Firebase
# VITE_FIREBASE_API_KEY=YOUR_FIREBASE_API_KEY
# VITE_FIREBASE_AUTH_DOMAIN=YOUR_FIREBASE_AUTH_DOMAIN
# VITE_FIREBASE_PROJECT_ID=YOUR_FIREBASE_PROJECT_ID
# VITE_FIREBASE_STORAGE_BUCKET=YOUR_FIREBASE_STORAGE_BUCKET
# VITE_FIREBASE_MESSAGING_SENDER_ID=YOUR_FIREBASE_MESSAGING_SENDER_ID
# VITE_FIREBASE_APP_ID=YOUR_FIREBASE_APP_ID

# --- POSTGRESQL CONFIGURATION ---
# Uncomment and configure if using PostgreSQL
# VITE_POSTGRESQL_CONNECTION_STRING=YOUR_POSTGRESQL_CONNECTION_STRING

# --- SQLITE CONFIGURATION ---
# Uncomment and configure if using SQLite
# VITE_SQLITE_DATABASE_PATH=YOUR_SQLITE_DATABASE_PATH

# --- OAUTH PROVIDER CREDENTIALS ---
# These are used when configuring OAuth-based protocols
# Each handshake can have its own credentials, but these serve as defaults
# VITE_DEFAULT_OAUTH_CLIENT_ID=YOUR_OAUTH_CLIENT_ID
# VITE_DEFAULT_OAUTH_CLIENT_SECRET=YOUR_OAUTH_CLIENT_SECRET

# --- API KEYS ---
# Default API keys for REST API Key protocol
# VITE_DEFAULT_API_KEY=YOUR_API_KEY

# --- CORS PROXY ---
# Optional: If you need to proxy requests to avoid CORS issues
# VITE_CORS_PROXY_URL=https://cors-anywhere.herokuapp.com/
